--- !ruby/object:RI::ClassDescription 
attributes: []

class_methods: []

comment: 
  - !ruby/struct:SM::Flow::P 
    body: A simple class to represent and create a PNG-File No drawing features given Just subclass and write [R,G,B]-Byte-Values into the <tt>@data</tt> matrix Build for compactness, so not much error checking!
  - !ruby/struct:SM::Flow::P 
    body: Code based on seattlerb's png, see http://seattlerb.rubyforge.org/png/
constants: 
  - !ruby/object:RI::Constant 
    comment: 
    name: CRC_TABLE
    value: "(0..255).map do |n|       (0...8).inject(n){|x,i| x = ((x & 1) == 1) ? 0xedb88320 ^ (x >> 1) : x >> 1}"
full_name: Compass::PNG
includes: []

instance_methods: []

name: PNG
superclass: Object
